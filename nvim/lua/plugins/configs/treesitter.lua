local options = {
  ensure_installed = { "lua", "go", "typescript", "html", "css", "python", "tsx", "javascript", "ruby", "rust" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
